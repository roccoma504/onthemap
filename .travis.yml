language: objective-c

branches:
 only:
 - master

xcode_project: onthemap.xcodeproj
xcode_scheme: onthemap
osx_image: xcode7.1
xcode_sdk: iphonesimulator9.1 

script: - xctool -project onthemap.xcodeproj -scheme onthemapTests build test -sdk iphonesimulator GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES
after_success: - bash <(curl -s https://codecov.io/bash)